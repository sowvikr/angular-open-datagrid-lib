import { HotKeysDirective } from './hot-keys.directive';

describe('HotKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new HotKeysDirective();
    expect(directive).toBeTruthy();
  });
});
